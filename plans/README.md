# Implementation Plans

Generated by the improve skill on 2026-07-08 against commit `13c252a`. Execute in
the order below unless dependencies say otherwise. Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row when done.

Repo verification commands: `npm run check` (Biome), `npm run test:no-watch`
(Vitest), `npm run build`. `npm run typecheck` exists only after plan 001 lands.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Repair the typecheck baseline and add CI | P1 | S | — | DONE (commits `cb82eb0` + `f16bf61`, merged into `feature/projection-form-ui` at `df45f2b` 2026-07-08) |
| 002  | Restore visible keyboard highlight in Select/DropdownMenu items | P1 | S | — (001 recommended) | DONE (commit `d4e718b`, merged into `feature/projection-form-ui` 2026-07-08) |
| 003  | Reconcile stale goal dates on localStorage restore | P1 | S | — (001 recommended) | DONE (commit `81bab23`, merged into `feature/projection-form-ui` at `d605fa1` 2026-07-08) |
| 004  | Bring CLAUDE.md and README back in line with the codebase | P2 | S | 001 | DONE (commit `e070636`, merged into `feature/projection-form-ui` 2026-07-08) |
| 005  | Dependency and package-manager hygiene | P2 | S | 001 | DONE (commits `a187d33..ccf65ce`, merged into `feature/projection-form-ui` at `fc11bc9` 2026-07-08) |
| 006  | Add the revised Harris-Benedict BMR formula | P3 | S | 001 | DONE (commit `a479891`, merged into `feature/projection-form-ui` 2026-07-08) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 first: it repairs `tsc --noEmit` (deprecated `baseUrl` currently fails it), adds the `typecheck` script every later plan's gates reference, and adds CI so subsequent changes are guarded.
- 004 depends on 001 because the refreshed docs reference the `typecheck` script.
- 005 depends on 001 because CI cements npm as the canonical package manager before `bun.lock` is deleted.
- 006 touches CLAUDE.md only if 004 already landed (see its Step 7).
- 002 and 003 are independent of each other and can run in parallel.

## Audited but not planned (backlog)

Findings from the same audit that the operator did not select for planning —
still valid, re-plan on request:

- **#5** Negative required-calories displayed verbatim (`goal-results.tsx:45`) — clamp or show an "unachievable timeline" message. S.
- **#6** i18n message drift: `formula.error` unused in code, missing in `de.json`; page metadata not localized; no key-parity test. S.
- **#8** Dead UI surface: `src/components/ui/input.tsx` + `switch.tsx` unused; unused `form.tsx` Form/Field machinery (app uses bare `Controller`); `separator.tsx` transitively dead; dead `group-data-[disabled=true]` selectors in `field.tsx`/`label.tsx`; mixed `forwardRef`/ref-as-prop; `radio-group.tsx`/`dropdown-menu.tsx`/`form.tsx` missing `'use client'`. M.
- **#9** `<html lang="en">` hardcoded for all locales; no next-intl middleware (locale negotiation absent; dead branch in `src/app/page.tsx:10`). M, MED risk (routing restructure).
- **#10** No integration tests for the days↔date sync (`goal-setting.tsx`) or restore/persist wiring (`projection-form.tsx`). M.
- Direction options not selected: imperial-units toggle (removed in `c1f2874`); shareable projection URLs.
- **New (found during 001 execution)**: `tsconfig.tsbuildinfo` is a tracked build artifact that every `tsc` run modifies (`incremental: true`) — should be gitignored and removed from tracking. S.

## Findings considered and rejected

- `npm audit` moderate advisory (postcss via next): the suggested "fix" downgrades Next to v9 — bogus resolution path, no reachable runtime impact. Not actionable.
- Enter-key implicit form submission on the buttonless `<form>`: HTML spec blocks implicit submission when a form has multiple fields and no submit button. Not a bug.
- No-op `useMemo` in `useProjection` (`useWatch` returns a fresh object each render): harmless, zero measured cost at this size.
- knip's "unused devDependency `shadcn`": false positive — it's the component CLI, used ad hoc.
